# Checkpoint

Собрать минимальный образ (`distroless` / `scratch`) для приложения: взять простое приложение (`Go`, `Python`, `Node`), собрать образ размером `< 20` MB, убрать `shell`, `package manager`, всё лишнее, настроить `healthcheck` без `bash`:

```bash

```
